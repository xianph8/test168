<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.test.test168">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/mAppTheme">
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.SlideActivity"/>
        <activity
            android:name=".ui.activity.RxJavaActivity"
            android:label="@string/title_activity_test_rx_java"
            android:theme="@style/mAppTheme"/>
        <activity android:name=".ui.activity.LitePalActivity"/>
        <activity
            android:name=".ui.activity.RecyclerViewActivity"
            android:label="@string/title_activity_recycle_view"
            android:theme="@style/mAppTheme"/>

        <service
            android:name=".service.MyIntentService"
            android:exported="false">
        </service>
        <service
            android:name=".service.TestIntentService"
            android:exported="false">
        </service>

        <activity android:name=".ui.activity.TestIntentServiceActivity">
        </activity>
    </application>

</manifest>