<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.test168">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/mAppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SlideActivity" />
        <activity
            android:name=".ui.RxJavaActivity"
            android:label="@string/title_activity_test_rx_java"
            android:theme="@style/mAppTheme" />
        <activity
            android:name=".ui.CommonAdapterActivity"
            android:label="@string/title_activity_common_adapter"
            android:theme="@style/mAppTheme" />
        <activity android:name=".ui.LitePalActivity"></activity>
    </application>

</manifest>